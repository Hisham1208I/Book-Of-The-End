<Window x:Class="BookOfTheEnd.Views.UpdateProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Downloading update"
        Width="420"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStyle="None"
        Background="{DynamicResource BackgroundBrush}"
        FontFamily="Segoe UI">
    <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
        <Grid Margin="22">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,14">
                <Border Width="40" Height="40" CornerRadius="10"
                        Background="{DynamicResource AccentMutedBrush}" Margin="0,0,12,0">
                    <TextBlock FontFamily="{StaticResource IconFont}" FontSize="18"
                               HorizontalAlignment="Center" VerticalAlignment="Center"
                               Foreground="{DynamicResource AccentBrush}" Text="&#xE896;" />
                </Border>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Downloading update" Style="{StaticResource H1}" />
                    <TextBlock Text="Book of the End will restart to finish installing."
                               Style="{StaticResource Caption}" Margin="0,2,0,0" />
                </StackPanel>
            </StackPanel>

            <ProgressBar x:Name="Bar" Grid.Row="1" Height="10" Minimum="0" Maximum="100" />

            <TextBlock x:Name="PercentText" Grid.Row="2" Style="{StaticResource Caption}"
                       Margin="0,8,0,0" HorizontalAlignment="Right" Text="0%" />
        </Grid>
    </Border>
</Window>
